application:
  config:
    security:
      enabled: true
    storage:
      path: models
      local: local
spring:
  profiles:
    include:
    - github
    - data
    - initializr
  application:
    name: zoran.io
  thymeleaf:
    enabled: false
eureka:
  client:
    register-with-eureka: false
    enabled: false